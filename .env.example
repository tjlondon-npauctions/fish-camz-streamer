# RPie-Streamer Environment Variables
# Copy to .env and edit as needed. Most config is done via the web UI.
# These are only needed if you want to override defaults before first run.

# Uncomment to override the web UI port
# WEB_PORT=8080
